import React from 'react';
import { useAuth } from './Login'; // Import useAuth hook from your Login component or context

const Profile = () => {

    return (
        <div>
            <h2>User Profile</h2>
            
        </div>
    );
};

export default Profile;
